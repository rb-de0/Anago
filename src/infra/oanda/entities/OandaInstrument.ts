export default class OandaInstrument {
    name: string
    pipLocation: number

    constructor(name: string, pipLocation: number) {
        this.name = name
        this.pipLocation = pipLocation
    }
}
